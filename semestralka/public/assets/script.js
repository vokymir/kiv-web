/*
 * Wasn't used central script, because all logic is embedded into views.
 * It's nothing more than really simple functions.
 * So the overhead for loading this file would be higher than simply writing it there.
 * */
